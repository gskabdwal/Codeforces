#include <bits/stdc++.h>
using namespace std;


void solve(){
    int n, x, y;
    string s;
    cin>>s;

    cout<<(s.size()+1)*25+1<<"\n";
    
    return;
}


signed main(){

    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int t = 1;
    //cin>>t;

    while(t--) solve();

    return 0;
}